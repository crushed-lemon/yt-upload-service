[project]
name = "yt-upload-service"
version = "0.1.0"
description = "Backend service for upload lifecycle of videos on lemonytube"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["app/src"]
